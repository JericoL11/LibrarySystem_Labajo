@model IEnumerable<LibrarySystem_Labajo.Models.BookCategory>

@{
    ViewData["Title"] = "Index";
    Layout = "_Layout_Users.cshtml";
}

<h1 class="text-white">Category</h1>

<p>
    <button class="btn btn-warning px-2 py-0"> <a class="text-black" style="text-decoration:none;"<a asp-action="Create" style="text-decoration:none">Create New</a></button>

</p>
<table class="table text-white">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.categoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.categoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.categoryId)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.categoryName)
            </td>

            <td>
                    <a class="text-warning" asp-action="Edit" asp-route-id="@item.categoryId">Edit</a> |
                    <a class="text-warning" asp-action="Details" asp-route-id="@item.categoryId">Details</a> |
                    <a class="text-warning" asp-action="Delete" asp-route-id="@item.categoryId">Delete</a>
               @*  <a asp-action="Edit" asp-route-id="@item.categoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.categoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.categoryId">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>
